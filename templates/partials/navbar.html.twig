<nav class="navbar navbar-expand-lg nav">
    <div class="container-fluid">
        <a class="nameNav" href="/"><img src="{{ asset('img/chaton.png')}}" width="50" alt="logo de t'chaton"> T'chaton</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link text-bg-dark rounded-pill" aria-current="page" href="{{ path('app_about') }}"><i class="fa-solid fa-paw"></i> A propos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-bg-dark rounded-pill" aria-current="page" href="{{ path('app_topic_index') }}"><i class="fa-solid fa-paw"></i> Les sujets</a>
                </li>
                {# //si c'est un admin il voit le btn #}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link text-bg-dark rounded-pill" aria-current="page" href="{{ path('app_comment_index') }}"><i class="fa-solid fa-paw"></i> Les commentaires</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <div class="d-flex">
            <ul class="navbar-nav">
            {% if app.user %}
                <li class="nav-item m-1">
                    <a class="nav-link text-bg-dark rounded-pill" aria-current="page" href="{{ path('app_logout') }}"><i class="fa-solid fa-poo"></i> Deconnexion</a>
                </li>
            {% else %}
                <li class="nav-item m-1">
                    <a class="nav-link text-bg-light rounded-pill" aria-current="page" href="{{ path('app_register') }}"><i class="fa-solid fa-cat"></i> Inscription</a>
                </li>
                <li class="nav-item m-1">
                    <a class="nav-link text-bg-light rounded-pill" aria-current="page" href="{{ path('app_login') }}"><i class="fa-solid fa-heart"></i> Connexion</a>
                </li>
            {% endif %}
            </ul>
        </div>
    </div>
</nav>